# 开发路线图（Development Roadmap）

## 当前状态：Phase 2 完成，Phase 3 即将开始

---

## Phase 0: 基础设施（✅ 已完成）

### 目标
建立异步事件驱动基础，支持多会话并发处理。

### 完成的工作
- ✅ InputBus（背压、优雅关闭）
- ✅ Adapter 统一接口
- ✅ SessionRouter（会话路由和隔离）
- ✅ SessionWorker（单会话执行引擎）
- ✅ SessionState（会话度量和状态）
- ✅ Session 空闲 GC（垃圾回收）

### 关键属性
- 多会话并发，串行处理
- 异步 I/O 友好
- 自动资源清理

### 代码位置
- `src/input_bus.py`
- `src/session_router.py`
- `src/session_state.py`
- `src/core.py`（_session_loop，_gc_loop）

---

## Phase 1: 脑干门控（✅ 已完成）

### 目标
实现快速、确定性的请求过滤和路由，保护系统不被过载。

### 完成的工作
- ✅ Gate MVP（7 阶段管道）
  - ✅ HardBypass（硬过滤）
  - ✅ FeatureExtractor（特征提取）
  - ✅ Scoring（评分）
  - ✅ Dedup（去重）
  - ✅ PolicyMapper（策略映射）
  - ✅ Finalize（最终化）
  - ✅ OutcomeAssembly（结果组装）
- ✅ SceneInferencer（场景分类）
- ✅ DropPool（DROP 状态管理）
- ✅ GateConfig（YAML 配置）
- ✅ GateConfigProvider（热更新）
- ✅ Gate + Worker 集成

### 关键属性
- 规则驱动，无 LLM
- 毫秒级响应
- 完全可配置
- 支持热更新

### 配置文件
- `config/gate.yaml`

### 代码位置
- `src/gate/` 目录（18 个文件）
- `src/config_provider.py`
- `src/core.py`（Gate 集成）

---

## Phase 2: 自主神经系统（✅ 已完成）

### 目标
实现系统自我保护和自我调节的闭环，支持 Agent 建议采纳。

### 完成的工作
- ✅ 痛觉系统（Nociception）
  - ✅ make_pain_alert()（痛觉生成）
  - ✅ extract_pain_key()（痛觉提取）
  - ✅ 痛觉常数定义
- ✅ SystemReflexController
  - ✅ handle_observation()（观察处理）
  - ✅ 痛觉聚合和突发检测
  - ✅ emergency_mode 自动激活
  - ✅ Agent 建议白名单过滤
  - ✅ TTL 管理和自动撤销
  - ✅ 冷却防抖
- ✅ 完整闭环集成
  - ✅ ALERT → pain aggregation → overrides → CONTROL 事件
  - ✅ Agent tuning_suggestion → whitelist filter → apply → TTL revert

### 关键属性
- 无 LLM 参与
- 规则驱动
- 自动恢复
- Agent 安全建议机制

### 代码位置
- `src/nociception.py`
- `src/system_reflex/` 目录（3 个文件）
- `src/core.py`（反射集成）

### 测试覆盖
- 30 个测试全部通过

---

## Phase 3: 认知智能（规划中 🚀）

### 目标
实现 Agent 的智能推理、规划、记忆和工具使用能力。

### 预期子任务

#### 3.1: IntentJudge（意图判断）
**职责**：理解用户的真实意图
- 分类意图类型（问询、命令、对话等）
- 提取实体和关键参数
- 判断优先级

**输入**：MESSAGE Observation（已通过 Gate 过滤）
**输出**：内部数据结构（不发出 Observation）

**约束**：
- 基于当前消息内容
- 不做长期历史推理（交给 Memory）
- 快速响应（< 100ms）

#### 3.2: QueryPlanner（查询规划）
**职责**：分解复杂查询为多步任务
- 分析意图，判断是否需要多步
- 生成执行计划
- 确定使用哪些 Tool

**输入**：Intent（来自 IntentJudge）
**输出**：Plan（内部结构）

**约束**：
- 不直接调用 Tool（交给 ToolRunner）
- 支持并行步骤识别

#### 3.3: Memory 结构优化
**职责**：建立记忆系统支持上下文理解
- 短期记忆（当前会话）
- 长期记忆（用户档案、历史）
- 记忆检索和更新

**输入**：当前会话历史
**输出**：相关的历史记录和用户档案

**约束**：
- 支持快速检索（< 50ms）
- 自动淘汰过期记忆
- 隐私敏感数据加密

#### 3.4: ToolRegistry 和 ToolRunner
**职责**：管理和执行外部工具/技能
- ToolRegistry：工具注册表
- ToolRunner：工具执行和结果处理
- ToolResult Observation：工具结果反馈

**工具示例**：
- 搜索（Web、知识库）
- 计算
- 数据库查询
- API 调用

**执行模式**：
- 同步工具（快速，< 500ms）
- 异步工具（慢速，>= 500ms）
- 并行工具（多个工具同时执行）

#### 3.5: 多模型策略选择器
**职责**：根据场景选择合适的模型
- 场景分析
- 成本与质量权衡
- 模型选择

**触发点**：
- IntentJudge 完成后
- 或 Agent 建议 force_low_model 时

### 实现顺序建议

```
3.1 IntentJudge
  ↓
3.3 Memory（支持上下文）
  ↓
3.2 QueryPlanner
  ↓
3.4 ToolRegistry + ToolRunner
  ↓
3.5 ModelSelector
```

### 架构设计

**单一 Agent 类**：
```python
class Agent:
    def __init__(self, config: AgentConfig):
        self.intent_judge = IntentJudge(...)
        self.memory = Memory(...)
        self.planner = QueryPlanner(...)
        self.tool_runner = ToolRunner(...)
        self.model_selector = ModelSelector(...)
    
    async def process(
        self, 
        obs: Observation,
        gate_config: GateConfig
    ) -> list[Observation]:
        """
        处理一个已通过 Gate 的观察，返回响应
        """
        intent = self.intent_judge.judge(obs)
        relevant_memory = self.memory.retrieve(intent)
        plan = self.planner.plan(intent, relevant_memory)
        results = await self.tool_runner.execute(plan)
        response = self.generate_response(results)
        return [Observation(...)]
```

**Worker 集成**：
```python
# 在 src/core.py 的 _session_loop 中
if outcome.decision.action == GateAction.DELIVER:
    # 新增：调用 Agent
    agent_outputs = await self.agent.process(obs, config)
    for output in agent_outputs:
        await self.bus.publish_nowait(output)
```

---

## Phase 4: 工具生态（远期规划 📋）

### 目标
丰富 Agent 的工具和能力库。

### 预期工作
- ✅ 搜索工具（Web、知识库、数据库）
- ✅ 计算工具（SQL、Python 沙箱）
- ✅ 数据处理工具
- ✅ 集成第三方 API
- ✅ 自定义工具开发框架

### 约束
- 所有工具调用都应该有超时保护
- 所有工具结果都重新进入 Gate
- 工具失败应该生成 ALERT（痛觉反馈）

---

## Phase 5: 可观测性和分析（远期规划 📊）

### 目标
建立系统的全面观测和分析能力。

### 预期工作
- ✅ Trace ID 传播
- ✅ 分布式追踪（Jaeger / Zipkin）
- ✅ 指标导出（Prometheus）
- ✅ 日志聚合（ELK Stack）
- ✅ 仪表板（Grafana）
- ✅ 成本分析

---

## 技术债务与优化（持续）

### 当前已知的改进空间

#### 1. Gate 规则增强
- [ ] 更复杂的评分算法（支持权重组合）
- [ ] 上下文感知的阈值（基于时间、用户群体）
- [ ] 动态规则加载（不需要重启）

#### 2. Memory 存储
- [ ] 支持不同的后端（文件、数据库、向量数据库）
- [ ] 异步存储操作
- [ ] 压缩和淘汰策略

#### 3. 性能优化
- [ ] Gate 缓存（热路径）
- [ ] 异步 I/O 优化
- [ ] 批量处理支持

#### 4. 可靠性
- [ ] 事务支持（确保状态一致性）
- [ ] 故障恢复（重启后的状态恢复）
- [ ] 备份和恢复

---

## 里程碑和时间估算

| 阶段 | 状态 | 预期时间 | 关键交付物 |
|------|------|---------|----------|
| Phase 0 | ✅ 完成 | 已完成 | 基础设施框架 |
| Phase 1 | ✅ 完成 | 已完成 | Gate 子系统 + YAML config |
| Phase 2 | ✅ 完成 | 已完成 | System Reflex + 30 测试通过 |
| **Phase 3** | 🚀 即将 | 3-4 周 | Agent 核心（Intent, Plan, Memory, Tool） |
| Phase 4 | 📋 规划中 | 4-6 周 | 工具库和集成 |
| Phase 5 | 📋 规划中 | 2-3 周 | 可观测性基础设施 |

---

## Phase 3 详细计划

### Week 1: IntentJudge + Memory 框架
- [ ] 定义 Intent 数据结构
- [ ] 实现 IntentJudge（基础版）
- [ ] 设计 Memory 接口
- [ ] 实现内存中的 Memory（支持检索）
- [ ] 编写单元测试

### Week 2: QueryPlanner + ToolRegistry
- [ ] 实现 QueryPlanner（基础版）
- [ ] 定义 ToolInterface 和 ToolResult
- [ ] 实现 ToolRegistry（工具注册）
- [ ] 编写集成测试

### Week 3: ToolRunner + 异步执行
- [ ] 实现 ToolRunner（同步和异步）
- [ ] 支持超时和错误处理
- [ ] ToolResult 反馈到 Gate
- [ ] 压力测试

### Week 4: Agent 完整集成 + ModelSelector
- [ ] 完整 Agent 类实现
- [ ] 与 Worker 集成
- [ ] 实现 ModelSelector
- [ ] 端到端测试

---

## 每个 Phase 的成功标准

### Phase 3 成功标准
- [ ] Agent 能处理至少 3 种不同的意图
- [ ] 支持至少 5 个常用工具
- [ ] 平均延迟 < 500ms（包括工具调用）
- [ ] 工具错误率 < 1%
- [ ] 至少 50 个新增测试，全部通过
- [ ] 架构文档完整

---

## 决策记录（ADR）

### ADR-1: Gate 不直接调用 Agent ✅
**决策**：Gate 返回 GateOutcome，Worker 负责执行决策
**原因**：解耦，Gate 保持轻量和快速

### ADR-2: overrides 是唯一调节接口 ✅
**决策**：系统状态变化只能通过更新 overrides 实现
**原因**：单一信息源，可审计，支持热更新

### ADR-3: Agent 只能建议，不能直接修改 ✅
**决策**：Agent 发送 tuning_suggestion，SystemReflex 评估后才应用
**原因**：安全性，防止 Agent 失控行为

### ADR-4: 所有状态变化都通过 Observation ✅
**决策**：没有隐藏的 API 调用或副作用
**原因**：可观测性，便于审计和测试

### ADR-5: 三层结构冻结 ✅
**决策**：脑干（Gate）、大脑（Agent）、自主神经（SystemReflex）的边界不再改变
**原因**：架构稳定性，为 Phase 3 铺路

---

## 接下来的行动项

### 立即可以做的
1. 运行 `uv run pytest -q` 验证基线（应该 30 passed）
2. 阅读 ARCHITECTURE.md 和各子系统文档
3. 确认三层结构的边界（本文档第 5 节）

### Phase 3 前的准备
1. 设计 Agent 的 Intent 和 Plan 数据结构
2. 评估 Memory 的后端选择（内存 vs Redis vs 向量数据库）
3. 列出优先级最高的 5 个工具

### 可选的优化（不影响 Phase 3）
1. 增强 Gate 的规则表达能力
2. 添加更多的监控指标
3. 性能基准测试（Gate 延迟）

---

## 相关文档

- [ARCHITECTURE.md](ARCHITECTURE.md) - 整体架构
- [GATE_SPECIFICATION.md](GATE_SPECIFICATION.md) - Gate 规范
- [SYSTEM_REFLEX_SPECIFICATION.md](SYSTEM_REFLEX_SPECIFICATION.md) - SystemReflex 规范
- [README.md](../README.md) - 项目概述

